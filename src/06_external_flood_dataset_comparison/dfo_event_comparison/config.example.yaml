# Example configuration for DFO event comparison.
# Copy this file to config.yaml and edit local paths before running scripts.

inputs:
  city_gumbel_event_table: "<set-local-path>"
  county_gumbel_event_table: "<set-local-path>"
  dfo_event_catalogue: "<set-local-path>"
  city_county_crosswalk: "<set-local-path>"

outputs:
  comparison_results_root: "<set-local-path>"

notes:
  - "DFO raw data and derived event-matching outputs are not included in this repository."
