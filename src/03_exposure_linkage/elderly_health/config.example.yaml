# Example configuration for elderly-health exposure-linkage workflow.
# Copy this file to config.yaml and edit local paths before running scripts.

project:
  output_root: "<set-local-path>"

charls:
  raw_root: "<set-local-path>"
  processed_root: "<set-local-path>"
  health_panel_60plus: "<set-local-path>"
  health_panel_with_index: "<set-local-path>"

flood_exposure:
  city_year_events: "<set-local-path>"
  city_rolling_exposure_root: "<set-local-path>"

linkage:
  city_code_crosswalk: "<set-local-path>"
  final_elderly_health_exposure_panel: "<set-local-path>"

analysis:
  return_periods: [2, 5, 10, 20, 50, 100]
  rolling_windows: [5, 10, 20, 30]
  survey_years: [2011, 2013, 2015, 2018, 2020]

notes:
  - "Raw CHARLS microdata are restricted and are not included in this repository."
  - "Exposure-linked individual-level analysis panels should remain local."
